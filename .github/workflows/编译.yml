name: Build All Platforms & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build ${{ matrix.platform }}-${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30  # 单个任务超时30分钟，避免无限等待
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          # Windows x64
          - os: windows-latest
            platform: windows
            arch: amd64
            ext: .exe

          # Linux x64 (glibc)
          - os: ubuntu-latest
            platform: linux
            arch: amd64
            ext: ''

          # Linux ARM64 (musl) - 使用 QEMU 模拟（不排队！）
          - os: ubuntu-latest
            platform: linux
            arch: arm64-musl
            ext: ''

          # macOS Intel
          - os: macos-13
            platform: macos
            arch: amd64
            ext: ''

          # macOS Apple Silicon (ARM64)
          - os: macos-14
            platform: macos
            arch: arm64
            ext: ''
      fail-fast: false  # 单个平台失败不影响其他平台

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      # Linux ARM64 (musl) - 完整功能 + QEMU 模拟优化
      - name: 构建 Linux ARM64-musl 版本（完整静态）
        if: matrix.arch == 'arm64-musl'
        uses: nick-fields/retry@v2  # 失败自动重试1次
        with:
          timeout_minutes: 25
          max_attempts: 2
          command: |
            # 1. 确保 QEMU 多架构支持已加载（关键！）
            docker run --rm --privileged multiarch/qemu-user-static --reset -p yes

            # 2. 拉取多架构 Alpine 镜像（确保包含 ARM64 层）
            docker pull alpine:latest

            # 3. 在 ARM64 容器中执行完整编译流程
            docker run --rm -v ${{ github.workspace }}:/app -w /app --platform linux/arm64 --memory 4g --cpus 2 alpine:latest sh -c "
              # 安装完整依赖（包括开发工具和静态链接工具）
              apk update
              apk add --no-cache \
                python3 py3-pip build-base musl-dev \
                zlib zlib-dev openssl openssl-dev libffi libffi-dev \
                staticx patchelf file lddtree

              # 使用虚拟环境（保持环境隔离，避免污染）
              python3 -m venv build_env
              source build_env/bin/activate

              # 安装 Python 依赖（带缓存优化）
              pip install --upgrade pip
              pip install --no-cache-dir -r requirements.txt
              pip install --no-cache-dir pyinstaller certifi urllib3

              # PyInstaller 完整打包（保留所有必要参数）
              python -m PyInstaller \
                --onefile \
                --name CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} \
                --clean \
                --console \
                --hidden-import requests \
                --hidden-import urllib3 \
                --hidden-import certifi \
                --hidden-import charset_normalizer \
                --hidden-import idna \
                --exclude-module tkinter \
                --exclude-module matplotlib \
                --exclude-module numpy \
                --exclude-module pandas \
                --exclude-module PIL \
                --exclude-module cv2 \
                --exclude-module scipy \
                --strip \
                --optimize 2 \
                cloudflare_speedtest.py

              # 二次处理：用 staticx 强制完全静态链接
              staticx dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}

              # 产物验证（保留关键检查，确保质量）
              echo '=== 产物文件信息 ==='
              file dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}

              echo '=== 动态依赖检查（应为：not a dynamic executable） ==='
              ldd dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} || true

              echo '=== 尝试运行帮助命令（验证可执行性） ==='
              ./dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} --help || true
            "

      # 其他平台 - 完整构建流程不变
      - name: 设置 Python 环境（非 ARM64-musl）
        if: matrix.arch != 'arm64-musl'
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: 安装依赖并构建（非 ARM64-musl）
        if: matrix.arch != 'arm64-musl'
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -r requirements.txt
          pip install --no-cache-dir pyinstaller certifi urllib3

          python -m PyInstaller \
            --onefile \
            --name CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }} \
            --clean \
            --console \
            --hidden-import requests \
            --hidden-import urllib3 \
            --hidden-import certifi \
            --hidden-import charset_normalizer \
            --hidden-import idna \
            --exclude-module tkinter \
            --exclude-module matplotlib \
            --exclude-module numpy \
            --exclude-module pandas \
            --exclude-module PIL \
            --exclude-module cv2 \
            --exclude-module scipy \
            --strip \
            --optimize 2 \
            cloudflare_speedtest.py

      # 产物处理 - 完整保留
      - name: 重命名 Windows 产物
        if: matrix.platform == 'windows'
        run: |
          move dist\CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}.exe dist\CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.ext }}

      - name: 赋予 Linux/macOS 执行权限
        if: matrix.platform != 'windows'
        run: chmod +x dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}

      - name: 上传产物到 Artifact（保留15天）
        uses: actions/upload-artifact@v4
        with:
          name: CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}
          path: dist/CloudflareSpeedTest-${{ matrix.platform }}-${{ matrix.arch }}${{ matrix.ext }}
          retention-days: 15

  # Release 步骤 - 功能完整，无精简
  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    permissions:
      contents: write
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 下载所有构建产物
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: 生成 Release 版本号
        id: get_version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "VERSION=${{ github.ref_name }}" >> $GITHUB_ENV
          else
            echo "VERSION=nightly-$(date +%Y%m%d-%H%M%S)" >> $GITHUB_ENV
          fi

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ env.VERSION }}
          tag_name: ${{ env.VERSION }}
          files: |
            artifacts/CloudflareSpeedTest-windows-amd64/CloudflareSpeedTest-windows-amd64.exe
            artifacts/CloudflareSpeedTest-linux-amd64/CloudflareSpeedTest-linux-amd64
            artifacts/CloudflareSpeedTest-linux-arm64-musl/CloudflareSpeedTest-linux-arm64-musl
            artifacts/CloudflareSpeedTest-macos-amd64/CloudflareSpeedTest-macos-amd64
            artifacts/CloudflareSpeedTest-macos-arm64/CloudflareSpeedTest-macos-arm64
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## 🚀 Cloudflare SpeedTest ${{ env.VERSION }} 发布
            
            ### 📦 下载链接
            - **Windows (x64)**：CloudflareSpeedTest-windows-amd64.exe
            - **Linux (x64, glibc)**：CloudflareSpeedTest-linux-amd64
            - **Linux (ARM64, musl)**：CloudflareSpeedTest-linux-arm64-musl
              - ✅ **完全静态链接，不依赖任何系统库**
              - ✅ 兼容 OpenWRT、Termux、Raspberry Pi 等 ARM64 设备
              - ✅ 通过 QEMU 模拟构建，无需排队原生 ARM64 runner
            - **macOS (Intel)**：CloudflareSpeedTest-macos-amd64
            - **macOS (Apple Silicon)**：CloudflareSpeedTest-macos-arm64
            
            ### 🌟 核心功能
            - 🎯 内置 97 个全球 Cloudflare 数据中心机场码
            - 🌍 支持城市名称智能识别（如输入"香港"自动匹配）
            - ⚙️ 3 种预设测试模式：快速/标准/高质量
            - 🔧 完全自定义测试参数（线程数、超时等）
            - 📊 自动生成 CSV 测试报告（result.csv）
            - 🆕 小白模式：只需输入 3 个数字即可快速测速
            
            ### 🚀 使用方法
            ```bash
            # Linux/macOS 用户
            chmod +x 下载的文件名
            ./下载的文件名
            ```
            Windows 用户直接双击运行即可。
            
            ### 📌 构建说明
            - ARM64-musl 版本采用 **QEMU 多架构模拟 + staticx 静态打包**
            - 确保在无 musl libc 和 zlib 的系统上也能运行
            - 构建时间约 15-20 分钟，无需等待原生 ARM64 runner 队列
